<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="Theme.MediArchive" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Keep empty: Compose MaterialTheme drives colors/typography -->
  </style>

  <style name="Theme.MediArchive.Starting" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splash_bg</item>
    <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
    <item name="postSplashScreenTheme">@style/Theme.MediArchive</item>
  </style>
</resources>
